import refs from "./refs";
import State from "./state";

refs.input.addEventListener("input", (e) => {
  //   state.state.places.filter((place) => {
  //     place.name.includes(e.target.value);
  //   });
  const a = new stateManager();

  console.log(a);
});
